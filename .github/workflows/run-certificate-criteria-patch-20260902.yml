name: Run certificate criteria patch 20260902

on:
  push:
    paths:
      - ".github/workflows/run-certificate-criteria-patch-20260902.yml"

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: python3 scripts/patch_certificate_criteria_20260902.py
      - name: Commit certificate criteria
        run: |
          set -euo pipefail
          if git diff --quiet -- curso-de-ingles-online/index.html; then
            echo "No changes to commit"
            exit 0
          fi
          git config user.name "Zoqvera Development Automation"
          git config user.email "actions@users.noreply.github.com"
          git add curso-de-ingles-online/index.html
          git commit -m "feat: add complete certificate criteria to course funnel"
          git push origin HEAD:main
